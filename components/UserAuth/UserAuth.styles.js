import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        borderColor: 'lightgrey', 
        borderRadius: 4,
        borderWidth: 1,
        marginBottom: 10,
        padding: 10,
        width: 250,
      },
  
      textSignUp: {
        color: '#689FEF',
        fontSize: 16,
        marginTop: 20,
        textAlign: 'center',
      }
})